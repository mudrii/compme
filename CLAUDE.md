## graphify

Optional: `graphify query|explain|path` CLI available (graph at graphify-out/) — use when it beats native search for cross-file/architecture questions. `graphify update .` refreshes it (AST-only, free).
