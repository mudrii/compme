[package]
name = "engine_core"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
context = { path = "../context" }
platform = { path = "../platform" }
ranker = { path = "../ranker" }
