[package]
name = "webconfig"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
# Exact pin: signature verification is consensus-critical — bump deliberately.
ed25519-dalek = "=2.2.0"
