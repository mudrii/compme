[package]
name = "webconfig"
version.workspace = true
edition = "2021"
license.workspace = true

[dependencies]
# Exact pin: signature verification is consensus-critical — bump deliberately.
ed25519-dalek = "=3.0.0"
