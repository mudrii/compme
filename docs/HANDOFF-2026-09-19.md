# Work handoff — 2026-09-19

Work is paused at the owner's request. This snapshot records where to resume;
it does not authorize further implementation or publication.
[ROADMAP.md](ROADMAP.md#authorized-implementation-queue--2026-09-19) remains
the single source of truth for pending work.

## Saved source state

- Branch: `main`.
- Commit: `f3cd4a4ee368239a7786e17f5c656c73ed89aa85` —
  `Implement platform safety fixes, memory controls, and Linux services`.
- Pushed to `origin/main`; remote SHA was verified after the push.
- The owner explicitly requested the commit/push after the incomplete gate
  results were disclosed. This is not a claim that every gate passed.
- GitHub CI for this commit has not been checked in this side conversation.
- Latest published release remains v0.1.6; no new tag or release was made.
- Existing untracked `.gate/` logs were preserved and excluded from the commit.
  This handoff is a subsequent local documentation addition.

The [implementation evidence](IMPLEMENTATION-EVIDENCE-2026-09-19.md) records
the pre-commit validation snapshot. Its statement that no commit was made
describes that earlier snapshot; the commit/push above happened afterward.

## Scope and status

The authorized batch was review-list items **1–6 and 19–35**. Windows feature
items **7–18 were excluded**. Consult the roadmap table for each item's exact
status and dependencies.

- Implemented: Linux mutation safety; Windows UIA/COM/identity repairs;
  instruction reconciliation; memory Off-mode erase, domain deletion and
  mode controls; Linux geometry, constrained XTEST insertion, tray and
  shortcuts; optional GPU feature forwarding; experimental AppImage tooling;
  release/CI hardening.
- Implementation does not close native acceptance: macOS Apps-pane behavior,
  Windows providers, and real Linux desktops still need evidence.
- Item 19 needs the old-Mac physical-hotkey baseline before the Carbon change.
- Items 27–28 need comparative GNOME/KDE/sway Wayland sessions and a selected
  strategy. Items 31–33 depend on usable adapters, package acceptance and the
  release environment.
- Item 29 still needs CUDA/Windows SDK verification. Item 30 remains failing
  or unmeasured by target. Item 35 remains blocked: inspected upstream
  llama-cpp-2 0.1.156 lacks the equivalent cancellation/lifetime API; retain
  the vendored 0.1.146 dependency, all three exact pins and both patches.

## Validation recorded before the commit

These results come from the committed evidence record; they were not rerun
while saving this handoff.

- Portable tests: **1,856 passed, 49 ignored**. Native Linux Xvfb/AT-SPI:
  **40/40 passed**, including the additional keyboard-group regression.
- Portable clippy and rustdoc passed with warnings denied. Windows MSVC and
  macOS aarch64 platform cross-checks passed. Astra's final review reported
  no remaining actionable finding in the implemented slices.
- Full Local Gate **did not pass**: formatting passed, then step **2/56**
  (`cargo clippy --locked --workspace --all-targets -- -D warnings`) failed
  on this Linux host: `objc2 only works on Apple platforms`, with Apple
  framework link-kind errors. The runner stopped; subsequent runner steps
  were not executed. Independent checks are not a substitute for this gate.
- Expected macOS workspace count was restamped to **2,222**; actual native
  enumeration and the full policy gate remain unverified.
- Linux warm-completion diagnostics failed the unchanged 500 ms budget:
  **CPU 1,778 ms**, **Vulkan 2,075 ms**. Vulkan executed on the Quadro RTX 4000.
  Concurrent builds make these diagnostic samples, not calibrated baselines.
- Ubuntu 24.04 AppImage build and extracted startup smoke passed. The artifact
  at `/tmp/compme-portable-target/compme-0.1.6-linux-x86_64-reviewed.AppImage`
  is temporary, experimental and not desktop-qualified or published.

## Resume sequence

1. Read `AGENTS.md`, `docs/ROADMAP.md`, and `docs/DEVELOPMENT.md`; inspect
   current git state before touching files. Preserve untracked `.gate/` logs.
2. Check CI for the exact pushed commit above. Address actual native-lane
   failures first, especially the macOS count/policy anchors. No native lane
   is proven green by cross-compilation.
3. Run the complete canonical `tools/dev/check.sh` gate on the appropriate
   host. Record any skipped or failed checks honestly; do not relax budgets.
4. Obtain the required GUI/hardware sessions for macOS memory controls and
   hotkey baseline, Windows UIA providers, and real Linux desktop acceptance.
   Record live evidence in `docs/ACCEPTANCE.md`.
5. Repeat CPU/Vulkan latency measurements on an idle host and investigate the
   budget failures; finish the missing backend/target checks.
6. Continue remaining authorized items according to roadmap dependencies.
   Windows items 7–18 require additional scope authorization. Follow the
   actual release runbook before any publication claim.

Work directly on `main`, with no branches or PRs. Future implementation
commits must satisfy the repository's documented gate requirements.
